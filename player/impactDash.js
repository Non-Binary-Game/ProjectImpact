function handleDoubleTap(player,cursors) {
   
    const { left, right, up } = cursors;
    console.log("hello")
    /*
    //time init
    const currentTime = new Date().getTime();
    let doubleTapDelay = 250;
    let lastTapTime = currentTime;
    // Check if the time difference between the current tap and the last tap is within the double tap delay
    if (currentTime - lastTapTime < doubleTapDelay) {
        // Double tap detected
        console.log('Double tap detected');
        // Add your double tap logic here

        // Reset last tap time
        lastTapTime = 0;
    } else {
        // Single tap detected
        console.log('Single tap detected');
        // Add your single tap logic here

        // Update last tap time
        lastTapTime = currentTime;
    }
    */
}